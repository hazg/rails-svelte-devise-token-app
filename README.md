# README

Rails application with svelte, svelte-i18n, rollup, and devise_token_auth.
Can login and register user.

Install and run
---------------
```bash
bundle
rake db:migrate
yarn
yarn run dev
rails s
```